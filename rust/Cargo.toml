[workspace]
resolver = "2"
members = [
    "shard-router",
    "fni-calc",
    "mesh-engine",
    "content-extractor",
    "stream-aggregator",
    "satellite-tasks",
]

[workspace.dependencies]
napi = { version = "2", features = ["napi8"] }
napi-derive = "2"
