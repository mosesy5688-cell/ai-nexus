@domain = https://ee56d064.ai-nexus-293.pages.dev

### 1. 提交 POST 请求 (Rating / 评论)
# 验证状态码 201 Created

POST {{domain}}/api/rating/model-test-001
Content-Type: application/json

{
    "rating": 5,
    "comment": "Testing 5-star rating on live deployment."
}

### 2. 测试速率限制 (应返回 429 Too Many Requests)
# 立即执行此请求，验证 Pages Function 的速率限制逻辑

POST {{domain}}/api/rating/model-test-001
Content-Type: application/json

{
    "rating": 1,
    "comment": "This rating should be rejected by 429 rate limiting."
}

### 3. 获取 GET 请求 (验证存储和计算)
# 验证状态码 200 OK，并检查平均分和总数是否正确 (如 5.0 和 1 票)

GET {{domain}}/api/rating/model-test-001