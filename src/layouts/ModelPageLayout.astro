---
import type { Model } from '@/types';
import BaseLayout from '@/layouts/BaseLayout.astro';
import AdSlot from '@/components/AdSlot.astro';
import ModelRatings from '@/components/ModelRatings.astro'; // Import the ratings component

export interface Props {
  model: Model;
}

const { model } = Astro.props;
const lastUpdatedDate = new Date(model.lastModified);
const formattedLastUpdated = !isNaN(lastUpdatedDate.getTime()) 
  ? lastUpdatedDate.toLocaleDateString() 
  : 'N/A';
console.log(`[ModelPageLayout] Model ID: ${model.id}`); // Log model ID
const visibleTags = model.tags?.slice(0, 15) || [];
const hasMoreTags = model.tags?.length > 15;
---
<BaseLayout title={model.name} description={model.description.substring(0, 160)}>
  <div class="container mx-auto px-4 py-12 md:py-16">
    <a href="javascript:history.back()" class="text-blue-600 dark:text-blue-400 hover:underline mb-8 inline-flex items-center gap-2 font-medium transition-colors group">
      <span class="transition-transform group-hover:-translate-x-1">&larr;</span> Back to previous page
    </a>
    
    <article class="bg-white dark:bg-gray-800/50 p-6 md:p-8 rounded-2xl shadow-xl border border-gray-200 dark:border-gray-700/50">
      
            <header class="border-b border-gray-200 dark:border-gray-700 pb-6 mb-8">
        <h1 class="text-4xl font-extrabold text-gray-900 dark:text-white leading-tight">{model.name}</h1>
        {model.author && <p class="text-lg text-gray-500 dark:text-gray-400 mt-2">by **{model.author}**</p>}
      </header>

            <div class="grid grid-cols-1 lg:grid-cols-4 gap-12 relative">
        
                <div class="lg:col-span-3">
          
                    {model.tags && model.tags.length > 0 && (
            <section class="mb-10">
              <h3 class="text-lg font-semibold mb-2 text-gray-800 dark:text-gray-200">Tags</h3>
              <div id="tags-container" class="flex flex-wrap gap-2">
                {visibleTags.map(tag => (
                  <span class="bg-blue-100 text-blue-800 text-sm font-medium px-3 py-1 rounded-full dark:bg-blue-900 dark:text-blue-300 transition-colors">{tag}</span>
                ))}
                {hasMoreTags && (
                  <button id="show-all-tags" class="bg-gray-200 text-gray-700 text-sm font-medium px-3 py-1 rounded-full dark:bg-gray-700 dark:text-gray-300 hover:bg-gray-300 dark:hover:bg-gray-600 transition-colors">
                    Show all ({model.tags.length})
                  </button>
                )}
              </div>
            </section>
          )}

          {model.summary_ai && model.summary_ai.length > 10 && (
            <section class="mb-8 p-4 bg-blue-50 dark:bg-blue-900/20 rounded-lg border border-blue-200 dark:border-blue-800 shadow-sm">
              <h3 class="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-2">AI Summary</h3>
              <p class="text-gray-700 dark:text-gray-300 text-base leading-relaxed">
                {model.summary_ai}
              </p>
            </section>
          )}

                    {model.readme ? (
            <section id="details-usage" class="mt-8 mb-12">
              <h2 class="text-3xl font-bold mb-4 text-gray-800 dark:text-gray-200 border-b pb-2">Details & Usage</h2>
              <div class="prose dark:prose-invert prose-lg max-w-none bg-gray-50 dark:bg-gray-900 p-6 rounded-lg prose-pre:bg-gray-200 dark:prose-pre:bg-gray-800 prose-pre:text-gray-800 dark:prose-pre:text-gray-200 border border-gray-100 dark:border-gray-700 shadow-inner">
                <div set:html={model.readme} />
              </div>
            </section>
          ) : (
            <p class="text-lg text-red-500 bg-red-50 dark:bg-red-900/20 p-4 rounded-lg border border-red-200 dark:border-red-800 mb-10">
              No detailed information (README) available for this model.
            </p>
          )}
        
        {/* User Ratings and Comments Section - Moved from aside to main content area */}
        <section class="mt-16">
          <div class="border-t border-gray-200 dark:border-gray-700 pt-12">
            <ModelRatings modelId={model.id} />
          </div>
        </section>
        
        </div>
        
                <aside class="lg:col-span-1">
          <div class="bg-gray-50 dark:bg-gray-700 p-6 rounded-lg sticky top-6 shadow-md border border-gray-100 dark:border-gray-600">
            <h3 class="text-2xl font-bold mb-4 text-gray-900 dark:text-white border-b pb-2">Model Details</h3>
            
                        <div class="space-y-3 text-gray-700 dark:text-gray-300">
              <div class="flex items-center gap-2">
                <span class="text-xl">⚙️</span>
                <span class="font-semibold">Task:</span>
                <span class="break-all">{model.task}</span>
              </div>
              <div class="flex items-center gap-2">
                <span class="text-xl">❤️</span>
                <span class="font-semibold">Likes:</span>
                <span>{model.likes.toLocaleString()}</span>
              </div>
              <div class="flex items-center gap-2">
                <span class="text-xl">⬇️</span>
                <span class="font-semibold">Downloads:</span>
                <span>{model.downloads.toLocaleString()}</span>
              </div>
              <div class="flex items-center gap-2">
                <span class="text-xl">📅</span>
                <span class="font-semibold">Updated:</span>
                <span>{formattedLastUpdated}</span>
              </div>
            </div>
            
                        {model.sources && model.sources.length > 0 && (
              <div class="mt-6 border-t border-gray-200 dark:border-gray-600 pt-6">
                <h4 class="font-bold text-lg mb-3 text-gray-900 dark:text-white">Sources & Repositories</h4>
                <ul class="space-y-2">
                  {model.sources.map(source => (
                    <li>
                      <a href={source.url} target="_blank" rel="noopener noreferrer" class="text-blue-500 hover:text-blue-600 dark:hover:text-blue-400 hover:underline transition-colors break-all flex items-center gap-1">
                        <span>{source.platform}</span>
                        <span class="ml-1 text-sm">&rarr;</span>
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </div>
        </aside>
      </div>
    </article>
  </div>

    <script define:vars={{ allTags: model.tags, hasMoreTags }}>
    // "Show all tags" button logic
    if (hasMoreTags) {
      const showAllBtn = document.getElementById('show-all-tags');
      const tagsContainer = document.getElementById('tags-container');

      if (showAllBtn && tagsContainer) {
        showAllBtn.addEventListener('click', () => {
          const remainingTags = allTags.slice(15);
          const tagsHtml = remainingTags.map(tag => 
            `<span class="bg-blue-100 text-blue-800 text-sm font-medium px-3 py-1 rounded-full dark:bg-blue-900 dark:text-blue-300 transition-colors">${tag}</span>`
          ).join('');
          
          const tempDiv = document.createElement('div');
          tempDiv.innerHTML = tagsHtml;

          tagsContainer.append(...tempDiv.childNodes);
          showAllBtn.remove();
        });
      }
    }
  </script>
</BaseLayout>