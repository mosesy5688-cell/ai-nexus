---
// src/components/model-detail/ModelNavigation.astro

interface Props {
  pipelineTag?: string;
  hasGguf?: boolean;
}

const { pipelineTag, hasGguf = false } = Astro.props;
---

<section class="mt-12 bg-white dark:bg-gray-800 rounded-xl p-8 border border-gray-200 dark:border-gray-700">
  <h2 class="text-2xl font-bold mb-6 text-gray-900 dark:text-white flex items-center gap-2">
    <span>🚀</span> What's Next?
  </h2>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <!-- Recommended Datasets -->
    <a href={`/explore?type=dataset&tag=${pipelineTag || 'text-generation'}`} 
       class="group p-6 bg-white dark:bg-gray-800 rounded-xl shadow-sm hover:shadow-md transition-all border border-gray-200 dark:border-gray-700">
      <div class="text-3xl mb-3">📊</div>
      <h3 class="font-bold text-gray-900 dark:text-white group-hover:text-indigo-600">Find Training Datasets</h3>
      <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
        Discover datasets compatible with this model
      </p>
    </a>

    <!-- Related Benchmarks -->
    <a href="/leaderboard" 
       class="group p-6 bg-white dark:bg-gray-800 rounded-xl shadow-sm hover:shadow-md transition-all border border-gray-200 dark:border-gray-700">
      <div class="text-3xl mb-3">📈</div>
      <h3 class="font-bold text-gray-900 dark:text-white group-hover:text-indigo-600">Compare Benchmarks</h3>
      <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
        See how this model ranks on standard tests
      </p>
    </a>

    <!-- Deployment Guide -->
    <a href="/knowledge/what-is-gguf" 
       class="group p-6 bg-white dark:bg-gray-800 rounded-xl shadow-sm hover:shadow-md transition-all border border-gray-200 dark:border-gray-700">
      <div class="text-3xl mb-3">⚡</div>
      <h3 class="font-bold text-gray-900 dark:text-white group-hover:text-indigo-600">
        {hasGguf ? 'Deploy with GGUF' : 'Learn About Deployment'}
      </h3>
      <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
        {hasGguf ? 'Run locally with llama.cpp or Ollama' : 'Understand deployment options'}
      </p>
    </a>
  </div>
</section>
