---
const { keyword } = Astro.props;
---
<a href={`/keyword/${keyword.slug}`} class="group block p-6 bg-white dark:bg-gray-800 rounded-lg shadow-md hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1">
    <div class="flex items-center justify-between">
        <div>
            <h3 class="text-xl font-bold text-gray-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                {keyword.title}
            </h3>
            <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">{keyword.count} models</p>
        </div>
        <div class="text-2xl opacity-50 group-hover:opacity-100 group-hover:text-blue-500 transition-opacity">
            →
        </div>
    </div>
</a>