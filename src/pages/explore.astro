---
import Layout from '../layouts/Layout.astro';
import allCategories from '../data/categories.json';

let pageTitle = "Explore AI Models - AI Nexus";
let pageDescription = "Browse and filter our extensive collection of open-source AI models. Sort by popularity, downloads, or freshness.";
---

<Layout title={pageTitle} description={pageDescription}>
    <div class="container mx-auto px-4 py-8 min-h-screen">
        <header class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900 dark:text-white">Explore Models</h1>
            <p class="text-gray-600 dark:text-gray-400 mt-2">Discover the best AI models from Hugging Face, PyTorch, and GitHub.</p>
        </header>

        <div class="flex flex-col lg:flex-row gap-8">
            <!-- Sidebar / Filters -->
            <aside class="w-full lg:w-64 flex-shrink-0 space-y-6">
                <!-- Search -->
                <div>
                    <label for="explore-search" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Search</label>
                    <div class="relative">
                        <input type="text" id="explore-search" placeholder="Keywords..." class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 outline-none transition-shadow" />
                        <svg class="w-5 h-5 text-gray-400 absolute right-3 top-2.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                    </div>
                </div>

                <!-- Sort -->
                <div>
                    <label for="explore-sort" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Sort By</label>
                    <select id="explore-sort" class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 outline-none">
                        <option value="likes">Most Popular (Likes)</option>
                        <option value="downloads">Most Downloaded</option>
                        <option value="last_updated">Recently Updated</option>
                    </select>
                </div>

                <!-- Category Filter -->
                <div>
                    <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">Categories</label>
                    <div class="space-y-4 max-h-96 overflow-y-auto pr-2">
                        {allCategories.map(group => (
                            <div key={group.group}>
                                <h4 class="text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase mb-2">{group.group}</h4>
                                <div class="space-y-2 ml-2">
                                    {group.items.map(category => (
                                        <label key={category.slug} class="flex items-start space-x-2 cursor-pointer group">
                                            <input 
                                                type="checkbox" 
                                                name="category" 
                                                value={category.slug} 
                                                class="mt-0.5 text-blue-600 focus:ring-blue-500 rounded" 
                                            />
                                            <span class="text-sm text-gray-700 dark:text-gray-300 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                                                {category.title}
                                            </span>
                                        </label>
                                    ))}
                                </div>
                            </div>
                        ))}
                    </div>
                </div>
            </aside>

            <!-- Main Content -->
            <main class="flex-1">
                <!-- Loading State with Skeletons -->
                <div id="explore-loading" class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
                    <!-- Skeleton placeholders (9 cards for explore page) -->
                    {Array.from({length: 9}).map(() => (
                        <div class="skeleton-card animate-pulse bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden border border-gray-100 dark:border-gray-700">
                            <div class="aspect-video w-full bg-gray-200 dark:bg-gray-700"></div>
                            <div class="p-5 space-y-4">
                                <div class="h-6 bg-gray-200 dark:bg-gray-700 rounded w-3/4"></div>
                                <div class="h-3 bg-gray-200 dark:bg-gray-700 rounded w-1/2"></div>
                                <div class="space-y-2">
                                    <div class="h-4 bg-gray-200 dark:bg-gray-700 rounded w-full"></div>
                                    <div class="h-4 bg-gray-200 dark:bg-gray-700 rounded w-5/6"></div>
                                </div>
                                <div class="flex justify-between pt-3 border-t border-gray-100 dark:border-gray-700">
                                    <div class="h-3 bg-gray-200 dark:bg-gray-700 rounded w-16"></div>
                                    <div class="flex gap-3">
                                        <div class="h-3 bg-gray-200 dark:bg-gray-700 rounded w-12"></div>
                                        <div class="h-3 bg-gray-200 dark:bg-gray-700 rounded w-12"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    ))}
                </div>

                <!-- Error State -->
                <div id="explore-error" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-6">
                    <strong class="font-bold">Error:</strong>
                    <span class="block sm:inline" id="explore-error-msg"></span>
                </div>

                <!-- Results Grid -->
                <div id="explore-grid" class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">
                    <!-- Populated by JS -->
                </div>

                <!-- No Results -->
                <div id="explore-no-results" class="hidden text-center py-12">
                    <p class="text-xl text-gray-600 dark:text-gray-400">No models found matching your criteria.</p>
                    <button id="reset-filters" class="mt-4 px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                        Reset Filters
                    </button>
                </div>
            </main>
        </div>
    </div>

    <script>
        // Client-side logic for Explore page
        import { renderModelCard } from '../scripts/render-model-card.js';
        
        let currentController = null;
        let debounceTimer = null;

        const elements = {
            search: document.getElementById('explore-search'),
            sort: document.getElementById('explore-sort'),
            categoryInputs: document.querySelectorAll('input[name="category"]'),
            grid: document.getElementById('explore-grid'),
            loading: document.getElementById('explore-loading'),
            error: document.getElementById('explore-error'),
            errorMsg: document.getElementById('explore-error-msg'),
            noResults: document.getElementById('explore-no-results'),
            resetBtn: document.getElementById('reset-filters')
        };

        function getFilters() {
            const q = elements.search.value.trim();
            const sort = elements.sort.value;
            const selectedCategories = Array.from(document.querySelectorAll('input[name="category"]:checked'))
                .map(input => input.value);
            return { q, sort, categories: selectedCategories };
        }

        async function fetchModels() {
            const { q, sort, categories } = getFilters();
            
            // Abort previous request
            if (currentController) currentController.abort();
            currentController = new AbortController();

            // UI State: Loading
            elements.grid.classList.add('opacity-50');
            elements.loading.classList.remove('hidden');
            elements.error.classList.add('hidden');
            elements.noResults.classList.add('hidden');

            try {
                const params = new URLSearchParams();
                if (q) params.set('q', q);
                params.set('sort', sort);
                // If categories selected, use tag filter (categories are stored as tags)
                if (categories.length > 0) {
                    // For multiple categories, we'll fetch with first one and filter client-side
                    // Or make multiple API calls - for simplicity, using first category
                    params.set('tag', categories[0]);
                }
                params.set('limit', '50'); // Fetch more for explore page

                const response = await fetch(`/api/search?${params.toString()}`, {
                    signal: currentController.signal
                });

                if (!response.ok) throw new Error(`API Error: ${response.status}`);
                
                const data = await response.json();
                let results = data.results || [];
                
                // Client-side filter for multiple categories
                if (categories.length > 1) {
                    results = results.filter(model => {
                        try {
                            const modelTags = JSON.parse(model.tags || '[]');
                            return categories.some(cat => modelTags.includes(cat));
                        } catch (e) {
                            return false;
                        }
                    });
                }
                
                renderModels(results);

            } catch (e) {
                if (e.name === 'AbortError') return;
                console.error("Fetch error:", e);
                elements.error.classList.remove('hidden');
                elements.errorMsg.textContent = e.message;
                elements.grid.innerHTML = '';
            } finally {
                elements.loading.classList.add('hidden');
                elements.grid.classList.remove('opacity-50');
            }
        }

        function renderModels(models) {
            if (models.length === 0) {
                elements.grid.innerHTML = '';
                elements.noResults.classList.remove('hidden');
                return;
            }

            // Use shared render function - eliminates 80+ lines of duplicate code!
            const html = models.map(model => renderModelCard(model)).join('');
            elements.grid.innerHTML = html;
        }

        // Event Listeners
        elements.search.addEventListener('input', () => {
            clearTimeout(debounceTimer);
            debounceTimer = setTimeout(fetchModels, 300);
        });

        elements.sort.addEventListener('change', fetchModels);
        
        elements.categoryInputs.forEach(input => {
            input.addEventListener('change', fetchModels);
        });

        elements.resetBtn.addEventListener('click', () => {
            elements.search.value = '';
            elements.sort.value = 'likes';
            document.querySelectorAll('input[name="category"]:checked').forEach(input => {
                input.checked = false;
            });
            fetchModels();
        });

        // Initial fetch
        fetchModels();

    </script>
</Layout>
