---
/**
 * /agents — 301 Redirect (agent entity type cancelled, merged into tool)
 *
 * The `agent` entity type was cancelled: GitHub agent frameworks + MCP servers
 * are reclassified as `tool` (the curated repos already overlap the tool index).
 * The agents catalog 301-redirects to /tools — a permanent, SEO-honest move (not
 * a soft-404). Reversible: restore the prior UniversalCatalog SSR page from git.
 */
return Astro.redirect('/tools', 301);
---
