---
/**
 * /prompts — 301 Redirect (prompt entity type cancelled)
 *
 * The `prompt` entity type (single-source LangChain Hub prompts) was retired.
 * LangChain content is now represented as agents, so the prompts catalog
 * 301-redirects to /agents — a permanent, SEO-honest move (not a soft-404).
 * Reversible: restore the prior UniversalCatalog SSR page from git history.
 */
return Astro.redirect('/agents', 301);
---
