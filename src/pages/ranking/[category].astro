---
/**
 * Category Ranking Page - V14.3 Redirect
 * 
 * V14.1 Iron Triangle: Consolidated /ranking/[category] → /[category]
 * Category pages now exist at root level (e.g., /text-generation)
 */

const { category } = Astro.params;

// Redirect to category page at root level
return Astro.redirect(`/${category}`, 301);
---
